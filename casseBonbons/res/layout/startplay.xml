<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".StartPlayActivity" >

    <Button
        android:id="@+id/nouvelle"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/button"
        android:text="Nouvelle partie" />

    <Button
        android:id="@+id/reprendre"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/nouvelle"
        android:layout_alignLeft="@+id/nouvelle"
        android:layout_marginBottom="51dp"
        android:background="@drawable/button"
        android:text="Continuer" />

    <Button
        android:id="@+id/niveaux"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/nouvelle"
        android:layout_below="@+id/nouvelle"
        android:layout_marginTop="55dp"
        android:background="@drawable/button"
        android:text="Niveaux" />
    
</RelativeLayout>
